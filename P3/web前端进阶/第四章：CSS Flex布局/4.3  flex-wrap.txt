4.3  flex-wrap


使用flex容器时，一行排不下的时候，项目会自动压缩挤在一条线上，
没有换行，也没有超出容器，而且即使给项目设置了宽度，也阻止
不了项目被压缩。如果想让项目换行，我们可以用flex-wrap属性，定义
项目是否换行、以及如何换行。
flex-wrap可能取三个值。
flex-wrap: nowrap(默认值) | wrap | wrap-reverse;
                 不换行              |  换行，第一行在上方 | 换行，第一行在下方

